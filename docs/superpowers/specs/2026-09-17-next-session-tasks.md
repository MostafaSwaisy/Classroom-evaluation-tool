# مهام الجلسة الجاية — 2026-09-17

الفرع: `fix/progress-and-design-fidelity` (45 دفعة فوق `main`)
آخر دفعة: `2a9eb77` — حجم النافذة

ابدأ من **مهمة 0**. الباقي مرتّب بالأولوية: T1 أهم إشي، T4 تحسينات.

---

## 0. تحقّق أول شي (٥ دقايق) ⚠️

المجموعة الكاملة **ما انشغلت** بعد آخر دفعتين (إصلاح rar + حجم النافذة).
الـ 64 اختبار اللي حواليهم خضر، بس المجموعة كلها لأ.

```bash
export QT_QPA_PLATFORM=offscreen
.venv/Scripts/python.exe -m pytest -q                              # المتوقع ~450+
.venv/Scripts/python.exe -m ruff check gui/ tests/ classroom_tool/
.venv/Scripts/python.exe run_gui.py --smoke ; echo $?              # لازم 0
grep -rniE '\b(push|upload|confirm|sync)\b|--confirm' gui/ | wc -l # لازم 0
```

لو طلع أحمر — صلّحه قبل أي شغل جديد. (دين `api.py`/`auth.py` — B023/B904 — معروف ومسجّل في
[BUILD_STATE.md](BUILD_STATE.md)، تجاهله.)

---

## T1 — أكمل قصة الـ RAR (نصّها بس خلص)

إصلاح `find_unrar()` بيشتغل على جهازك لأن WinRAR مثبّت. بس **الأداة لسه ما بتقول
للمصحح إنها محتاجته**، وهاي بالضبط الفجوة اللي ضيّعت ٦ تسليمات بصمت.

### T1.1 — `doctor` يفحص UnRAR
`classroom_tool/doctor.py` حالياً صفر إشارة لـ unrar (`grep -c unrar` = 0).

- ضيف فحص في قسم «الملفات»: `find_unrar()` رجّع إشي أو لأ
- لو لأ → نتيجة تحذير (مش فشل — الـ zip لسه بيشتغل) مع `fix_action` بيشرح الحل
- اربطه بشاشة «الاتصالات وفحص الجاهزية»
- ⚠️ `_STAGE_COUNT = 3` في `doctor.py` — لو ضفت مرحلة عدّلها، وإلا البار بيكذب

### T1.2 — وثّقها
`README.md` كمان صفر إشارة. ضيف سطر: .rar محتاج UnRAR / WinRAR، والأداة بتلاقيه
تلقائياً في أماكن التثبيت المعتادة أو من PATH.

### T1.3 — شاشة التحضير: زر إجراء على تنبيه `no_unrar`
لوحة «تنبيه المراجع» بتعرض النص. خليها تعرض زر بيودّي لشاشة الجاهزية لما يكون
السبب `no_unrar` — نفس نمط زر «أصلح student_id_pattern» في [pull.py](../../../gui/screens/pull.py).

---

## T2 — وفاء التصميم: باقي الشاشات

شاشة `prepare` صارت النموذج المرجعي — شوف [prepare.py](../../../gui/screens/prepare.py)
والدفعة `f43e845`. الويدجتات جاهزة: `StatCard` و`FilterTabs` و`Card.add_widget(w, stretch)`.

**اشتغل شاشة شاشة، وكل وحدة بدفعتها.** اقرأ `design/screens/<slug>.png` قبل ما تبدأ.

### T2.1 — `tracking_report` (الأبعد عن التصميم)
ناقص مقابل [tracking-report.png](../../../design/screens/tracking-report.png):
- صف KPI: إجمالي الطلاب · معدل التسليم · المعدل التراكمي · المتأخرات
- تابات فلترة: الكل / المستقرون / تحت المتابعة / مكتمل 100%
- **at-risk threshold slider** — `compute_status()` بياخد `risk_threshold` أصلاً،
  فالبيانات موجودة؛ حرّك المؤشر → أعد الحساب
- كروت الطلاب المعرضين للتعثر تحت الجدول
- الموجود حالياً: كرتين + جدول + رسمين بيانيين

### T2.2 — `assignments`
كرت واحد + chip واحد، بلا بحث ولا تابات. التصميم بده قائمة أغنى + فلترة.

### T2.3 — `dashboard`
كرت واحد. التصميم بده صف KPI + إجراءات سريعة.
🔴 **افحص هاي بعينك أول** — لقطة التشغيل بيّنت فراغ عمودي كبير تحت الكروت.

### ما لازم تنقله من الـ mockups
الـ mockups فيها بيانات مخترعة. القاعدة: **عمود بيعرض قيمة مخترعة أسوأ من عمود مش موجود.**
أمثلة رفضناها في `prepare`: عمود «الترميز UTF-8» (الأداة عمرها ما بتفحص ترميز)،
وأزرار إجراءات لكل صف.

---

## T3 — ديون من BUILD_STATE (صغيرة ومستقلة)

مناسبة لما يكون وقتك قصير. التفاصيل في [BUILD_STATE.md](BUILD_STATE.md) تحت «Carry».

- [ ] `DataTable` — مؤشر الترتيب بيبقى قديم بعد أي `set_rows`
- [ ] `DataTable.set_rows` — حارس طول لـ `row_keys` (IndexError محتمل)
- [ ] `roster.py` — الفلترة لصفر نتائج بترسم شبكة فاضية. **الحل جاهز:**
      نمط `_no_match` في [prepare.py](../../../gui/screens/prepare.py) — انسخه
- [ ] `roster_read.py` — `\t` حرفي بيضل بين الرقم والاسم في `_missing.txt`
- [ ] `QCheckBox.stateChanged` مهجور في Qt 6.7+ → `checkStateChanged` (كنسة على كل المشروع)
- [ ] `doctor.reset_token()` لسه بيطبع وبيرجّع `None` — بده نتيجة مركّبة
- [ ] `roster.py` `_open_selected` — سياق التنقّل بيحمل `student_id` فاضي لكل صفوف الـ fixture

---

## T4 — تحسينات اختيارية

- **عدّادات صفحات** لـ `courses_aliases` / `assignments` / `roster` / `grades_draft`.
  حالياً إلها نص تحميل صادق بس بلا عدّاد — لأنها نداء API واحد أو قراءة xlsx محلية.
  لو بدك عدّاد حقيقي، لازم `api._paginate` يرسل tick لكل صفحة.
- **`prepare` ما بتعرض تقرير تشغيل سابق** — لو رجعت للشاشة بعد تحضير خلص، بتبدأ من
  النموذج. بدها دالة مسح لـ `extracted/` في الـ backend.
- **أعد توليد مصفوفة الحالات** بعد تغييرات التصميم:
  `.venv/Scripts/python.exe dev/state_matrix.py <out>` ثم مرّة عين على الفاتح/الغامق (P5-U4).

---

## قبل أي دمج على `main`

لسه مفتوح من BUILD_STATE:
1. مراجعات Opus الإلزامية: R1 · R7 · R8 · R9
2. نقاط تفتيش Fable للمراحل 3 و4 و5
3. P5-U4 — مرّة عين على الفاتح/الغامق

---

## ملاحظات بيئة (وفّرت وقت، خليها بالبال)

- **الخط العربي ما بينرسم في `QT_QPA_PLATFORM=offscreen`** — بتطلع مربعات في اللقطات.
  هاي البيئة مش عيب في الكود. للقطة حقيقية شغّل بلا offscreen وصوّر الشاشة بـ PowerShell.
- **heredocs في bash بتاكل `\n` و`\\`** — أي patch script فيه سلاسل مهربة، اكتبه بملف
  ونفّذه، أو استخدم `Edit` مباشرة. ضيّعت وقت على هاي أربع مرات.
- **git-bash بيمسخ المسارات العربية** للأدوات الخارجية (`tar.exe` شاف `????_1`).
  مرّر المسارات من Python مش من الشل.
