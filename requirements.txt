# --- CLI / backend ---
google-api-python-client>=2.120.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
openpyxl>=3.1.2
click>=8.1.7
tqdm>=4.66.0

# --- config / rubrics (comment-preserving round-trip) ---
ruamel.yaml>=0.18    # R10 (config.py) / R7 (rubrics); replaces PyYAML

# --- GUI (Phase 0+) ---
PySide6>=6.7
keyring>=25          # OS credential store for the optional Provider-A API key (R8)
