# مساحة عمل التدريس — UCAS

مجلد واحد فيه كل شي متعلق بسحب واجبات Google Classroom وتصحيحها.
أنت (Claude Code) بتشتغل هون كمساعد تصحيح، مش كمصحّح مستقل.

---

## القاعدة الأولى: أنت ما بترفع درجات

كل شغلك بينتهي عند **مسودة** المصحح لازم يراجعها.
ممنوع منعاً باتاً:
- تشغيل أي أمر فيه `push` أو `--confirm`
- تعديل `_roster.xlsx` مباشرة
- حذف أي إشي من `submissions/`

المصحح (مصطفى) هو اللي بيراجع `grades_draft.xlsx` وبيرفع بنفسه.

---

## بنية المجلد

```
.
├── cli.py                  ← أوامر Classroom (sحب، فك ضغط، متابعة)
├── classroom_tool/         ← الكود
├── tools/write_grades.py   ← يحوّل JSON درجات → xlsx
├── rubrics/                ← معايير التصحيح لكل واجب
├── config.yaml             ← اختصارات المساقات (لا تعدّله بدون إذن)
└── submissions/            ← المخرجات (خارج git)
    └── SE2026/HW03/
        ├── _roster.xlsx    ← كشف التسليم (لا تعدّله)
        ├── _index.md       ← فهرس الملفات — ابدأ من هون
        ├── _missing.txt
        ├── files/          ← الملفات كما نُزّلت
        ├── extracted/      ← الأرشيفات مفكوكة
        └── grades_draft.xlsx  ← مخرجك
```

---

## سير العمل الكامل

لما المصحح يقول "اسحب واجب PHP من مساق كذا وصحّحه":

### 1. حدّد المساق والواجب
```bash
python cli.py work PHP2026
```
اعرض القائمة واسأل أي واجب إذا العنوان مش واضح. **لا تخمّن.**

### 2. اسحب
```bash
python cli.py pull PHP2026 "HW03"
```

### 3. جهّز
```bash
python cli.py prepare "submissions/PHP2026/HW03_..."
```
هاد بيفك الـ zip وبيبني `_index.md`.

### 4. اقرأ المعايير
شوف `rubrics/` — إذا في ملف للواجب استخدمه. إذا مافي، **اسأل المصحح** عن توزيع الدرجات قبل ما تبدأ. لا تخترع معايير من راسك.

### 5. صحّح
اقرأ `_index.md`، بعدين اقرأ ملفات كل طالب وقيّمها حسب المعايير.
اشتغل على دفعات 10 طلاب وأعطِ تحديث بينها.

### 6. اكتب المسودة
اكتب `grades.json` بالصيغة المطلوبة، بعدين:
```bash
python tools/write_grades.py "submissions/PHP2026/HW03_..." grades.json
```

### 7. سلّم
قول للمصحح وين الملف، وأعطِ ملخص: المتوسط، أعلى/أدنى، وأي حالات محتاجة انتباهه.

---

## قواعد التصحيح

**كن محدد.** "الكود ممتاز" ما بتفيد الطالب. "الـ route محددة صح بس الـ controller فيه منطق أعمال لازم ينتقل لـ service class" بتفيد.

**الملاحظات بالعربي**، المصطلحات التقنية بالإنجليزي. هاي لغة الطلاب.

**علّم الحالات المشكوكة بدل ما تقرر فيها.** استخدم حقل `flags`:
- تشابه واضح بين تسليمين → `"تشابه مع 120210456"`
- ملف فاضي أو تالف → `"الملف ما بينفتح"`
- تسليم خارج نطاق الواجب → `"سلّم واجب تاني"`
- كود يبدو منسوخ من AI بدون فهم → `"يحتاج مراجعة شفوية"`

ما تقرر إن طالب غشّ. اعرض الملاحظة وخلي المصحح يقرر.

**لما تتردد، أعطِ الدرجة الأعلى وعلّم بـ flag.** خطأ لصالح الطالب أهون من العكس، والمصحح رح يراجع.

**لا تصحّح ملف ما قدرت تقرأه.** اترك الدرجة `null` وحط flag.

---

## معايير جودة الكود (PHP / Laravel)

عند تقييم "جودة الكود" في واجبات Laravel، هاي المؤشرات المعتادة:
- منطق الأعمال في Controllers بدل Services/Actions
- استعلامات داخل حلقات (N+1)
- غياب validation على المدخلات
- مفاتيح أو كلمات سر مكتوبة في الكود
- SQL خام بدون binding
- تسمية غير متسقة (camelCase مقابل snake_case)
- كود مكرر بدل استخدام Blade components

بس **المعايير في `rubrics/` هي المرجع** — هاي القائمة للاسترشاد بس.

---

## أوامر مرجعية

| الأمر | الوظيفة |
|---|---|
| `python cli.py courses` | قائمة المساقات |
| `python cli.py work COURSE` | واجبات مساق |
| `python cli.py pull COURSE "HW"` | تحميل التسليمات |
| `python cli.py pull COURSE "HW" --no-files` | كشف بدون تحميل |
| `python cli.py prepare DIR` | فك الضغط + فهرس |
| `python cli.py status COURSE` | تقرير متابعة الطلاب |

---

## ملاحظات بيئة

- ويندوز — استخدم `.venv\Scripts\activate`
- الاتصال ممكن ينقطع؛ لو فشل `pull` في النص، أعد تشغيله (بيكتب فوق القديم)
- إذا طلع `401` أو `invalid_grant`: التوكن انتهى، شغّل `python cli.py auth`
- ملفات الطلاب بالعربي — الترميز UTF-8 دايماً
